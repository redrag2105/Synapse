/// Shared layout constants for the bottom navigation bar.
class AppBottomNavMetrics {
  AppBottomNavMetrics._();

  static const double iconSlotHeight = 42;
  static const double labelSlotHeight = 20;
  static const double expandedContentHeight = iconSlotHeight + labelSlotHeight;
  static const double stickyContentHeight = iconSlotHeight;
}
